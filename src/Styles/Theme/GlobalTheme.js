import { createTheme } from '@mui/material/styles';

export const tema = createTheme({
  palette: {
    boton: {
      main: '#334257',
    },
    secondary: {
      light: '#ff7961',
      main: '#f44336',
      dark: '#ba000d',
      contrastText: '#000',
    },
  },
});
